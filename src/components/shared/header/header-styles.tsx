import * as colors from '../../../utils/styles/colors';

export const header = () => ({
  height: 50,
  width: '100%',
  backgroundColor: colors.white,
  borderBottom: `1px solid ${colors.borderGray}`,
  boxShadow: `3px 3px 3px 3px ${colors.shadowGray}`,
});