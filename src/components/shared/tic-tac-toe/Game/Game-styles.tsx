import { CSSProperties } from 'glamorous';

export const game = {
  display: 'flex',
  flexDirection: 'row',
} as CSSProperties;

export const gameInfo = () => ({
  marginLeft: 20,
});

export const gameBoard = () => ({

});